<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>URL Shortener</title>

    <style>
        .heading {
            font-family: serif;
            font-weight: 700;
            background-image: linear-gradient(to right, #43cea2, #185a9d);
            word-spacing: 30%;
            height: 60px;
            padding: 7px;
            border-radius: 7px;

        }

        .inputbox {
            border: 2px solid rgb(114, 114, 114);
        }
        .tablerow{
            font-family: Verdana,Tahoma, sans-serif;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="heading" style="text-align: center;">URL SHORTENER</h1>
        <form action="/create" method="POST" class="my-4 form-inline">
            <label style="font-weight: 600;font-size: 20px;" for="fullURL" class="sr-only">URL</label>
            <input required placeholder=" Paste the URL" type="url" name="fullUrl"
                class="inputbox form-control col mr-2" id="fullUrl">
            <input type="submit" value="Short" class="btn btn-success mt-2">
        </form>
        <table class="table table-striped table-responsive">
            <thead style="font-size: 18px;">
                <tr class="tablerow">
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i< urlResult.length; i++) { %>

                    <tr>
                        <td><a href="<%= urlResult[i].longUrl %>" target="_blank">
                                <%= urlResult[i].longUrl %>
                            </a></td>
                        <td><a href="http://localhost:5000/<%= urlResult[i].shortUrl %>"
                                target="_blank">http://localhost:5000/<%= urlResult[i].shortUrl %></a></td>
                        <td>
                            <%= urlResult[i].clickCount %>
                        </td>
                        <td><a href="/delete/<%=  urlResult[i].id %>" class="btn btn-sm btn-danger">Remove</a></td>
                    </tr>
                    <% } %>
            </tbody>
        </table>

    </div>
</body>

</html>